{
  "name": "tw-us-stock-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "台股與美股分析、回測與紙上交易研究介面",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check:syntax": "find js tests scripts -type f -name '*.js' -print0 | xargs -0 -r -n1 node --check",
    "check:static": "node scripts/check-static.js",
    "check:ui": "node scripts/check-ui.js"
  },
  "engines": {
    "node": ">=24"
  }
}
