/* Public runtime config for Pages deployment (carries no confidential material).
   Default empty = Fugle mode unavailable. Deployment replaces the assignment
   below with the public proxy URL; this file must NEVER carry provider keys.
   Allowed keys: MARKET_DATA_PROXY_URL and public feature flags only. */
globalThis.__MARKET_DATA_PROXY_URL__ = "";
